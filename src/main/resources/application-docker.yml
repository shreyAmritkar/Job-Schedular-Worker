spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/job_scheduler
    username: scheduler
    password: scheduler
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080
